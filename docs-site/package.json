{
  "name": "cap-upgrade-docs",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "npm run sync-data && vitepress dev docs",
    "build": "npm run sync-data && vitepress build docs",
    "preview": "vitepress preview docs",
    "sync-data": "cp ../changes.json docs/public/changes.json"
  },
  "devDependencies": {
    "mermaid": "^11.12.3",
    "vitepress": "^1.3.0",
    "vitepress-plugin-mermaid": "^2.0.17"
  }
}
